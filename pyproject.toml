[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pay-day"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.5",
    "requests>=2.34.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pay_day"]
